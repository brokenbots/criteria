package engine

// node_subworkflow.go — Subworkflow invocation at runtime (W13, Phase 3).

// This package will contain runSubworkflow in a future implementation.
// For now, subworkflow invocation is pending W14 (universal step target).
